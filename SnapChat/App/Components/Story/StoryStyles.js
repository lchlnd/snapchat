import { StyleSheet } from 'react-native'

export const storyStyles = StyleSheet.create({
  container: {
    flexDirection:'column',
    flex:1
  },
  heading: {
    color:'purple',
    fontSize:20,
    padding:10
  }
})